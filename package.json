{
  "name": "hebrew-web-app",
  "version": "1.0.0",
  "description": "A comprehensive web application for learning Hebrew, featuring an interactive frontend and a robust backend API.",
  "type": "module",
  "scripts": {
    "test": "node --experimental-specifier-resolution=node tests/run-all-tests.js",
    "docs:check:links": "node scripts/ci/check-doc-links.mjs",
    "docs:check:api": "node scripts/ci/check-api-doc-coverage.mjs",
    "docs:check:coupling": "node scripts/ci/check-api-doc-change-coupling.mjs",
    "docs:check": "npm run docs:check:links && npm run docs:check:api",
    "ci:build": "npm --prefix backend ci && npm --prefix backend run build && npm --prefix frontend-react ci && npm --prefix frontend-react run build"
  },
  "author": "Hebrew Web App Team",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "yaml": "^2.4.1",
    "node-fetch": "^3.3.2"
  }
}
